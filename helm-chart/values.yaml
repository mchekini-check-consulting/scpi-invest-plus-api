image:
  tag: latest
  repo: mchekini
  name: scpi-invest-plus-api

application:
  environment: int

database:
  url: jdbc:postgresql://scpi-invest-db:5432/postgres
  username: postgres
  password: postgres

kafka:
  bootstrapServers: kafka-svc.kafka:9094

security:
  oauth2:
    issuerUri: https://keycloak.check-consulting.net/realms/master
    jwkSetUri: https://keycloak.check-consulting.net/realms/master/protocol/openid-connect/certs

jpa:
  hibernate:
    ddlAuto: validate
    dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure: health,prometheus
  prometheus:
    enabled: true